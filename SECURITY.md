# 安全策略

## 支持范围

当前维护版本为 `1.2.x`。安全修复优先落在当前版本；更早版本不承诺单独回补。

## 报告漏洞

请优先使用 GitHub 仓库的 **Security → Report a vulnerability** 私密报告入口。不要在公开 Issue、讨论区或 Pull Request 中披露尚未修复的漏洞细节。

报告中请包含：

- 受影响版本和 Windows/Codex/Node.js 版本。
- 可复现步骤与预期、实际结果。
- 影响范围，例如任意文件写入、命令注入、路径逃逸或敏感数据暴露。
- 可安全分享的日志或最小复现；提交前移除账号、路径中的个人信息和对话内容。

如果私密报告入口不可用，请先通过仓库维护者的 GitHub 主页建立私下联系，不要公开发布利用代码。

## 安全边界

管理器不应请求管理员权限，不应修改 Codex 官方安装文件，也不应读取或记录 API Key、登录状态和对话内容。任何突破这些边界的行为都应按安全问题报告。
